# Changelog

## v1.5.14

### 改善

- **色合わせ（Oklab）に「暗部の明るさ」スライダーと「自動調整」ボタンを追加**
  - テクスチャの最も暗い部分を目標色の明るさの何割まで落とすかを調整できます
  - 白など明るい色を指定すると影が中間グレーまで落ちて銀髪っぽく見えていた問題は、自動調整の式の見直しで解消（白で0.7、パステルも明るめに）。純赤・純青などの高彩度色は深い影のルックをほぼ維持します（0.3→0.4）。中間明度・暗色の挙動は従来どおりです
  - 従来は目標色を変えるたびに内部で自動再計算されていましたが、スライダーで設定した値が勝手に変わらないよう、自動計算はセットアップ時と「自動調整」ボタンを押したときのみに変更しました

### UI 改善

- **ヘルプの表示方法を変更**: 先頭の「ヘルプ」トグルで全説明を一括表示する方式をやめ、各項目・セクションの行末の「？」ボタンで説明を個別に開閉する方式に（開くのは同時に1つ）
- 説明文の表記を整理: 「target」→「目標色」への統一、モード説明の見出しを選択肢の表記（色指定（デフォルト）/ グラデーション / RGB差分）に一致、句読点・「できます」表記の統一（日本語）

## v1.5.13

### 不具合修正

- **マットキャップ統一がメッシュ統合OFFのときアップロード後に反映されない問題を修正**
  - プレビュー（Unity上の表示）ではマットキャップ画像が統一されるのに、実ビルド（VRChatアップロード）では数値設定のみコピーされ画像がコピーされていなかった
  - ビルド経路でもプレビューと同じくマットキャップ画像（`_MatCapTex` / `_MatCap2ndTex`、None含む）を基準マテリアルから上書きするように
  - リムライト等の数値・色設定は従来から正しく反映されていました（テクスチャのみが対象の不具合）

## v1.5.12

### 新機能（マットキャップ / 発光 / 反射 / ラメ マスクの再配置生成）

- **統合後の UV に再配置したマットキャップ / 発光 / 反射 / ラメのマスクを PNG として生成できるように**
  - 対象: `_MatCapBlendMask` / `_MatCap2ndBlendMask` / `_EmissionMap` / `_EmissionBlendMask` / `_Emission2ndMap` / `_Emission2ndBlendMask` / `_SmoothnessTex` / `_MetallicGlossMap` / `_ReflectionColorTex` / `_GlitterColorTex`
  - 従来はメッシュ統合後も元の UV 座標を参照したままで、マットキャップや発光の模様がズレていた
  - インスペクタのテクスチャ設定に「マスクテクスチャを生成」ボタンを追加。素材で使われているマスクを統合後 UV に再配置した PNG を `CHM_Generated` フォルダに書き出します
  - マスクが無い素材も「その素材で機能が有効か」を白 / 黒（発光は透明）で焼き込むため、「一部の房だけマットキャップ / 発光」の構成も 1 枚のマスクで再現できます
  - **自動では適用されません。**「割り当て」ボタンでマテリアル設定（プレビューマテリアル）に適用するか、PNG を編集してから手動で割り当ててください（機能トグルも同時に ON になります）
  - 元の髪で使われていたマットキャップ画像の一覧もインスペクタに表示し、選んで割り当てられます
  - 生成後に UV 配置や素材が変わった場合は再生成を促す警告を表示します
  - **圧縮形式を用途に合わせて明示設定**: マットキャップマスク・スムースネス・メタリックはシェーダがアルファを読まないため DXT1 固定（メモリ半減）、エミッション / 反射色 / ラメ色はアルファ保持（DXT5）。ミップストリーミングも有効化
  - 一覧の表記は lilToon のマテリアル設定 UI と同じ「セクション名 / 欄名」（例: マットキャップ/マスク）
  - **各マスクの解像度をインスペクタから変更可能**（512 / 1024 / 2048 / 4096）。実体はインポート設定なので再生成不要・PNG 自体はフル解像度のまま。生成時の既定は 512〜2048 にクランプ（4096 アトラスでもマスクは 2048 から）
- **プレビューマテリアル生成時にマットキャップのマスク / カスタムノーマルをコピーしないように**
  - 統合後の UV とズレたマスクがそのまま出力に乗る原因だったため（マットキャップ画像自体は従来どおりコピー）
  - あわせて、マットキャップ設定統一（unifyMatCap）でもマスクは各素材自身の物を維持するように
- **制限事項**
  - エミッションの UV モードが UV0 以外、またはスクロール / 回転アニメ使用時は生成対象外（警告を表示）。ラメの色テクスチャも UV モードが UV0 以外は対象外
  - ラメの形状テクスチャ（`_GlitterShapeTex`）はラメ粒子空間でサンプリングされるため対象外
  - 反射の ON / OFF 塗り分けで OFF の房を黒にするのは近似です（完全な無効化は `_ReflectionColorTex` の透明側で行われます）
  - エミッションマスクのアルファ（発光強度）は保持されます。インポート設定は必要に応じて調整してください
  - 発光色などの数値設定はマスクでは表現できないため、マテリアル設定側の値になります

### UI 改善

- メッシュ統合ON時、「テクスチャ設定」を基本設定と同列のセクションに変更（既定は閉じた状態）
- 「色変更対象」をインスペクタから非表示に（通常の利用では変更不要なため。`_MainTex` 以外を色合わせ対象に追加する機能自体は維持しており、Inspector の Debug モードから編集可能）。これに伴いメッシュ統合OFF時は「テクスチャ設定」セクション自体が表示されなくなります
- 「UV配置」（読み取り専用の旧形式データ表示）をインスペクタから非表示に（実際のUV配置はセットアップウィンドウのアイランド配置で管理されており、表示に情報価値がないため）

### 内部整理

- 未使用コードを削除（動作への影響なし）: 旧 Renderer 単位配置のアトラス生成経路（`BuildAtlasForSlot` / `GetOrCalculatePlacements` / `CalculateAutoLayout` / 旧 Blit 2種）、`PreviewGenerator.cs`、`TextureAtlasPass` 内の未使用マットキャップコピー

## v1.5.11

### 改善（テクスチャメモリ削減）

- **AO / ノーマルマップが使われていない場合、アトラスを生成しないように**
  - 従来はソースに AO / ノーマルマップが 1 枚も無くても、全白 / フラットノーマルのアトラスをフル解像度で生成していた（2048 設定で約 10.7MB の無駄）
  - 生成をスキップし、統合マテリアルの該当スロットもクリアするように（見た目は変わりません）
- **不透明なカラーアトラスをアルファ無しの形式（DXT1）で圧縮するように**
  - アルファチャンネルが実質使われていない場合、テクスチャメモリが半分になります（2048 設定で約 2.7MB 削減）
  - 半透明を含む場合は従来通り DXT5。アルファを特殊用途に使うシェーダ向けに「アルファチャンネルを常に保持」設定を追加
- **AO アトラスの圧縮を DXT5 → DXT1 に**
  - AO はグレースケールのためアルファ・色情報が不要。メモリ半減で品質影響なし
- **ノーマル / AO アトラスの解像度をメインアトラスと別に指定できるように**
  - テクスチャ設定に「ノーマルマップ解像度」「AO マップ解像度」（×1 / ×1/2 / ×1/4）を追加
  - Quest / Android 向けビルドにもそのまま効きます（圧縮形式は従来通り ASTC）

## v1.5.10

### 修正

- **鮮やかな色を指定したときに、陰影のグラデーションに帯状のムラが出る問題を修正**
  - sRGB で表現できない色を、従来は彩度を 90% ずつ最大 8 回まで縮めて収めていた
  - 縮小回数が整数のため出力の彩度が 9 段階の飛び飛びの値しか取れず、明暗の変わり目に沿って階段状の段差が出ていた
  - 彩度を二分探索で連続的に求めるようにし、明度と色相を保ったまま表現範囲ぎりぎりまで収めるように
  - あわせて、8 回縮めても収まらない色を強制的に切り詰めていたために色相がずれる問題も解消
  - **鮮やかなターゲット色（金・赤・青・シアン等）では暗部やハイライトの階調が変わります。** 落ち着いた茶・アッシュ・黒はほぼ変化しません
- **塗り感統一で、模様のないテクスチャにザラつきが乗る問題を修正**
  - 模様のほとんど無いテクスチャに適用すると、8bit の量子化ノイズを最大 80 倍に増幅していた
  - ディテール量が量子化ノイズと区別できない場合は効果を掛けないように
  - 通常の毛束模様があるテクスチャでは出力は変わりません
- **グラデーションモードの色が暗く出る問題を修正**
  - Linear color space プロジェクトで、グラデーション用テクスチャの色空間指定が抜けていたため、GPU 処理時に暗くなっていた（HSV／Oklab など他モードは影響なし）
- **ノーマルマップアトラスの法線が僅かに狂う場合がある問題を修正**
  - ノーマルマップのアトラスがリニアではなく sRGB 扱いになっていたため、法線の向きがずれることがあった
  - PC 向けビルドは影響なし。プレビュー表示と Quest / Android 向けビルドで改善
- **セットアップを再実行すると Chimera Hair Master が重複作成される問題を修正**
  - 同じメッシュを対象にした Chimera Hair Master が既にある場合、二重に作成されて表示が壊れていた
  - 既存のものを検出したら通知し、新規セットアップを中止するように
- **メッシュ変形：測地線距離＋一定フォールオフでブラシ範囲の外側まで変形する問題を修正**
  - 表示されるブラシ球の最大2倍の範囲が変形・保存されていた
- **メッシュ変形：編集セッション中に「変形を適用」した際にデルタが二重に焼き込まれる問題を修正**
  - 焼き込み前に編集を確定してから処理するように
- **メッシュ変形：対称編集の適用範囲を明示**
  - 対称は移動・膨張のみに反映される旨をインスペクターに表示（回転・スケール・スムーズの対称化は今後の課題）
- **メッシュ変形：対称編集中にブラシ半径を変えると対称側が追従しない問題を修正**
- **マスクプレビュー：メッシュ統合OFF＋メッシュ変形時にマスクが誤った位置に表示される問題を修正**
- **その他の軽微な修正**：Renderer 削除時の色ピッカー選択、UVアイランド全解除時のギズモ残留、Prefab 未保存時のインスペクター例外

### その他

- ガマットマッピングとバンドゲインの回帰テストを追加

## v1.5.9

### 機能追加

- **Quest / Android 向けビルドのテクスチャ圧縮に対応**
  - ビルドターゲットが Android の場合、生成テクスチャ（アトラス・色合わせ）を ASTC で圧縮するように
  - 従来は Android でも DXT5/BC5・非圧縮(RGBA32)のまま出力され、アップロード時に「Avatar validation failed」となる場合があった
  - Windows(PC) 向けビルドは従来通り（DXT5/BC5）で変更なし

### 修正

- **メッシュ統合OFF時に lilToon の輪郭線設定がプレビュー・ビルドに反映されない問題を修正**
  - lilToon の輪郭線はシェーダ切替で表現されるため、統合OFFの経路でも輪郭線の有無を揃えるように（各パーツのレンダリングモードは維持）
- **生成テクスチャに多重圧縮由来のブロックノイズが乗る問題を修正**
  - 中間処理はすべて非圧縮で通し、最終出力時に一度だけ圧縮するように
- **メッシュ統合OFFのプレビュー／ビルド品質を改善**（明度オフセット・ブラー/シャープ・塗り感統一の適用漏れ等）
- **統合対象外のサブメッシュのUVがアトラス生成時に壊れる場合がある問題を修正**
- **色変換キャッシュが別領域（代表色の異なるUV領域）の結果を取り違える場合がある問題を修正**
- **特定プロパティの色変更を明示的にOFFにした設定が一部経路で無視される問題を修正**

### その他

- **同一マテリアルを複数パーツで共有しつつパーツ別設定（明度／ブラー／マスク）を付けた場合に警告を表示**
  - 手動の適用／Prefab出力では最初のパーツの設定のみ反映されるため（NDMFビルドは個別に適用）
- テクスチャ圧縮チェーンの回帰テストを追加

## v1.5.8

### 修正

- **「塗り感統一」使用時にテクスチャメモリが大幅に増える問題を修正**
  - 塗り感統一の最終合成テクスチャが `RGBA32` のまま Material に残る経路を修正し、通常の色変換と同じく元テクスチャ形式へ圧縮するように
  - 塗り感統一を後段で適用する場合は中間テクスチャの圧縮を遅延し、不要な二重圧縮を避けるように
- **塗り感統一の中立 band に 8bit 量子化由来の微小ノイズが乗る問題を修正**
  - `RGBA32` の中立値を `0.5` ではなく `128/255` として扱い、フラットなテクスチャを正しく無変化として処理

### 内部

- 塗り感統一後の出力形式と中立 band の回帰テストを追加

## v1.5.7

### 修正

- **NDMF 1.12.0 以下の環境でコンパイルに失敗する問題を修正**
  - v1.5.6 で NDMF 1.13.0 以降の `RenderGroup.WithData(data, equality)` を使用していたため、依存解決で古い NDMF が入っている環境では `CS1501: No overload for method 'WithData' takes 2 arguments` が発生していた
  - NDMF 1.12.0 以下にも存在する `WithData(data)` 呼び出しに戻し、`package.json` の `nadena.dev.ndmf >=1.7.0` と実装の互換性を合わせた

## v1.5.6

### 機能追加

- **メッシュ変形が MeshRenderer（MeshFilter）にも対応**（※スタンドアロンのメッシュ変形コンポーネント）
  - 従来 SkinnedMeshRenderer 専用だった変形編集・保存・プレビュー・ビルドを、MeshFilter を持つ MeshRenderer でも利用可能に
  - メッシュアクセスを `RendererMeshAccess` に統一。MeshRenderer は Blendshape 非対応のため Blendshape 出力 UI は自動で無効化

### 修正

- **メッシュ統合で合計頂点数が 65535 を超えると統合メッシュが破損する問題を修正**
  - 統合メッシュ・Remaining メッシュに 32bit インデックス（`IndexFormat.UInt32`）を設定
- **メッシュ変形の保存・Prefab出力で頂点数が変わったメッシュに古いデルタを焼き込む破損を防止**
  - ビルドパスと同じ頂点数チェックを保存・出力経路にも追加（不一致時は焼き込まず中止）
- **Renderer 削除/並べ替え時に色合わせ無視マスク・塗り感統一のお手本・変形編集インデックスがズレる漏れを修正**
  - `RendererIndexSynchronizer` の再マップ対象に colorMasks / strandPatternSettings.referenceRendererIndex / deformEditingRendererIndex を追加
- **対象リストの途中の Renderer を削除すると後続パーツに設定がズレる問題を修正**
  - セットアップウィンドウの対象リストで途中のパーツを「×」で削除すると、除外設定・メッシュカットマスク・UV配置の参照インデックスが詰められず、削除位置より後ろの Renderer に他パーツの設定が適用され、末尾パーツの設定が失われていた
  - 特にメッシュカットマスクのズレはビルド時に意図しない髪パーツのポリゴンを削除する破綻につながっていた
  - 削除時にインデックスを正しく詰める処理を追加（Inspector 側の `RendererIndexSynchronizer` 相当をウィンドウ側にも適用）
- **描画モードの異なる対象パーツがセットアップ直後に非表示になる問題を修正（v1.4.0 退行）**
  - 基準マテリアルが半透明/カットアウトで、対象に不透明マテリアルのパーツ（例: 耳・しっぽ）を含めると、セットアップ直後のプレビューで該当パーツが消えていた。ビルド（アップロード）・Prefab 出力・色合わせ適用も同様
  - 原因は v1.4.0 で `ApplyShaderSettings` に追加されたシェーダー本体の強制差し替え。基準マテリアルのシェーダー（半透明等）が対象マテリアルに適用され、アルファ前提で作られていないテクスチャが全ピクセル破棄されていた
  - シェーダー本体は対象側を維持し、数値設定のみ統一する v1.3.2 までの挙動に復元
  - 付随する仕様変更: previewMaterial の輪郭線 ON/OFF をシェーダーごと対象へ伝播する挙動（v1.4.0 追加）は、メッシュ統合なしモードでは無効に戻る（メッシュ統合モードは基準マテリアルの完全クローンを使うため従来どおり反映される）

### 内部

- `Editor/AssemblyInfo.cs` を追加（テストアセンブリへの `InternalsVisibleTo`）
- `ApplyShaderSettings` の回帰テスト `TextureAtlasPassTests` を Tests/Editor に追加

## v1.5.5

### 機能追加

- **「マスクを作成」ボタンと Inspector の連携を追加**
  - メッシュ切除マスク／色合わせ無視マスクの「マスクを作成」ボタンから開いたマスクツールで保存すると、Inspector の対応する submesh のマスクフィールドに**自動で割り当て**られるように
  - 既にマスクが割り当てられている場合は上書き
  - マスクツール v1.2.2 以降が必要（古いマスクツールでは従来どおり通知なし＝デグレなし）

### 内部改善

- `MaskToolRegistry` に保存後コールバック対応 API を追加（v1.3+ 用、後方互換）
- `MaskToolLauncher.OpenMaskToolForSubmesh` に `Action<Texture2D>` オーバーロード追加

## v1.5.4

### 修正

- **「色合わせ無視マスク」が PNG 出力経路に反映されない問題を修正**
  - **「色合わせ適用」ボタン**で出力された PNG にマスクが適用されず、マスクで黒く塗った領域も色変換が掛かっていた
  - **「Prefab 出力」**で生成された Prefab 内の PNG も同上
  - プレビュー / ビルド (NDMF) では正常に反映されていたが、PNG 出力経路だけ取りこぼしていた
- **「塗り感統一」が「Prefab 出力」に反映されない問題を修正**
  - v1.5.3 で「色合わせ適用」ボタンには対応したが、「Prefab 出力」経由は取りこぼしていた

### 内部改善

- 色合わせ無視マスクの適用ロジックを `ColorMaskApplier.TryApply` に集約
  - ビルド / プレビュー / PNG 出力 / Prefab 出力すべてが共通実装を使う形に統一
  - 経路追加時の処理漏れを構造的に防止

## v1.5.3

### 修正

- **塗り感統一の対象範囲を拡大**
  - **アトラスモードのプレビュー**で、お手本 Renderer に「ぼかし／シャープ」「明度オフセット」「色合わせ無視マスク」が設定されている場合に塗り感統一が適用されない問題を修正（v1.5.2 で修正したビルド側と同じバグがプレビューにも残っていた）
  - **「適用（PNG 出力）」ボタン**経由でも塗り感統一が反映されるよう対応（従来はビルド・プレビューでは反映されるが PNG 出力には反映されない不整合があった）
- 既存の警告 `CS8600` (`PrefabExporter.cs`) を解消

### 内部改善

- 塗り感統一のロジックを `StrandPatternApplier` に集約（`PrepareRefData` / `TryComposeStrand`）
  - ビルド / プレビュー (アトラス・非アトラス両方) / PNG 出力すべてが共通実装を使う形に統一
  - 関連コードを net -300 行縮小
- 色変換キャッシュを再利用する経路を追加し、お手本に blur/sharp がない場合のプレビュー再描画コストを v1.5.2 同等に維持

## v1.5.2

### 修正

- **塗り感統一が一部条件下でビルドに反映されない問題を修正**
  - お手本 Renderer に「ぼかし／シャープ」または「明度オフセット」が設定されていると、ビルド時に塗り感統一が完全にスキップされていた
  - ターゲット Renderer 側に同上の調整、または「色合わせ無視マスク」が設定されていると、その Renderer だけ塗り感統一がスキップされていた
  - プレビューでは適用されていたが GestureManager 等のビルド結果には反映されない状態だった
  - ビルド側の塗り感統一適用をプレビューと同じ inline 方式に統一して解消
  - 副次的に、同じテクスチャを複数 Renderer で共有しているケースで Renderer ごとに正しく ratio 計算されるようになった

## v1.5.1

### 改善

- **対象選択UIの簡素化**
  - 自動検出されたアバターを表示する read-only フィールド・案内ラベル・到達不能だった `NoAvatarDescriptor` 警告を削除
  - 髪パーツが配置されているのにアバターが検出できない場合のみ警告 HelpBox を表示
  - 4 言語対応 (ja / en / zh-Hans / ko) — `Window:TargetSelection:NoAvatarDetected` にリネーム

## v1.5.0

### 新機能

- **塗り感統一**
  - お手本 Renderer の質感（線の細さ・塗りの濃淡）を他 Renderer に転送
  - ラプラシアンピラミッドで 2 バンド（細・中）に分解し、各バンドのコントラスト振幅を独立に rescale
  - target 自身の毛束位置・密度は維持したまま、メリハリだけ reference に寄せる
  - Inspector「色合わせ」セクション内に新セクション「塗り感統一」を追加
    - 有効化トグル、お手本 Renderer、線の細さ強度、塗りの濃淡強度、対象スケール
  - ビルド・プレビュー両対応（cache 経路 / temp material 経路）
  - 4 言語対応 (ja-JP / en-US / zh-Hans / ko-KR)
  - 既知の限界: 周波数帯域内のスペクトルは変えないため、構造変換（細い strand を太くする等）はできない
- **メッシュ変形を Blendshape として出力**
  - メッシュ変形 Inspector の保存ボタン上に「Blendshape として出力」トグル + 名前フィールド
  - 「メッシュを保存」/「メッシュを保存して入れ替え」時に焼き込みではなく Blendshape として末尾追加
  - 既定 Blendshape 名: `CHMDeform`（同名衝突時は `CHMDeform_1` のように unique 化）
  - 入れ替え時は `SetBlendShapeWeight(100)` で見た目維持 + deltas クリア
  - VRChat アニメーション/エクスプレッションから weight を動かして変形 ON/OFF・部分適用可能
  - エクスポート（一括）・Prefab 出力でも本トグル設定を尊重
  - 4 言語対応 (ja / en / zh-Hans / ko)
  - 既知の制約: ビルドパス (NDMF MeshDeformPass) は焼き込み専用。Blendshape 化したい場合はビルド前に手動で「保存して入れ替え」する運用（トグル ON で未入れ替えなら警告ログ）

### 改善

- **「塗りの細かさを調整」→「ぼかし・シャープ調整」にリネーム**
  - 実装内容（負の値でブラー、正の値でシャープ）を直接表現する名称に変更
  - 4 言語対応 (ja / en / zh-Hans / ko)
- **メッシュ変形「メッシュを保存」ダイアログの初期フォルダを元メッシュ隣に**
  - 元メッシュと同じフォルダがデフォルト保存先になり、クリック数が削減
- **編集中にメッシュ変形コンポーネントを削除しても Mesh が Missing にならないように**
  - `ChimeraHairMaster` / `MeshDeformationStandalone` に `[ExecuteAlways]` + `OnDestroy` を追加
  - 編集中削除時に renderer.sharedMesh を originalMesh に自動復元

### API 変更

- `IMeshDeformationTarget` に `ExportAsBlendshape` / `BlendshapeName` プロパティ追加
- `MeshDeformer.ExportDeformedMeshAsBlendshape(renderer, deformation, name, out actualName)` 追加

### 内部

- `Runtime/StrandPatternSettings.cs` 新規
- `Editor/Shader/StrandPatternHighPass.compute`, `StrandPatternCompose.compute` 新規
- `Editor/Processing/StrandPatternExtractor.cs`, `StrandPatternComposer.cs`, `StrandPatternApplier.cs` 新規
- `Editor/NDMF/ColorTransformPass.cs`, `ChimeraHairMasterPreview.cs` に塗り感統一適用フックを追加
- Tests/Editor に `StrandPatternExtractorTests.cs`, `StrandPatternComposerTests.cs` 追加

## v1.4.4

### 改善

- **アバター自動選択のヒント表示を追加**
  - 対象選択セクションのアバター欄直下に「※アバターは髪パーツから自動選択されます」を常時表示
  - v1.4.0 以降アバタードロップが不要になったことを明確化し、問い合わせを削減
  - 4 言語（ja-JP / en-US / zh-Hans / ko-KR）対応

## v1.4.3

### 改善

- **Prefab出力を色変換オフ時にも利用可能に**
  - 従来は色変換 ON 時のみ「エクスポート」セクションが表示されていたが、色変換オフ時も Prefab 出力を可能化
  - 色変換オフ時の Prefab 出力では「マテリアル統一（任意）・メッシュ変形反映（任意）・不要 bone 整理」が実行される
  - 「不要 bone 整理だけ目的の独立 Prefab 化」「メッシュ変形だけ確定した Prefab」などのユースケースに対応
- **Prefab出力に「マテリアル設定を統一する」トグル追加**
  - デフォルト ON（従来挙動と同等）
  - OFF 時は基準マテリアルからの shader 設定コピーをスキップし、各 Renderer の元マテリアル設定をそのまま保持
  - `ChimeraHairMaster.unifyMaterialSettings` フィールドとしてアバターごとに永続化

## v1.4.2

### 新機能

- **Prefab出力機能**
  - 色合わせ・マテリアル設定・変形情報を反映した髪パーツのPrefabを生成
  - 元マテリアル / 元テクスチャ / 元アバターは書換しない非破壊エクスポート
  - 元マテリアル単位で重複排除した clone マテリアルを `{元名}_CHM.mat` として元マテリアル同フォルダに保存
  - 必要な bone・PhysBone・Constraint を含む単独 Prefab を生成
  - ModularAvatar 検出時は各髪 Armature root に `MA Merge Armature` を自動付与（asmdef versionDefines による条件コンパイル）
  - 生成 Prefab はアバターと同じシーンのルート直下に自動配置
- **Inspector「エクスポート」セクション追加**
  - 既存「テクスチャ出力」と新規「Prefab出力」を 1 つの Foldout に統合
  - 「非破壊処理が不要な場合、他ツールと併用して問題がある場合はエクスポートしてから試してください」のヒントを表示
  - サブセクション「Prefab出力」には「メッシュ変形を適用」トグル（デフォルト ON）

### 改善

- ローカライズ拡充
  - エクスポート関連の文字列を 4 言語（ja-JP / en-US / zh-Hans / ko-KR）に対応

### 内部整理

- `ColorApplier` の以下メソッドを `internal` に格上げ（`PrefabExporter` から再利用）
  - `DetermineSourceColor`、`GetRendererBrightnessOffset`、`GetRendererBlurSharp`、`CopyTextureImportSettings`、`DecompressTexture`、`CopyMatCapTextures`
- `Editor/Processing/HierarchyDependencyResolver.cs` 新規追加（bone・PhysBone・Constraint 依存解決と不要 GameObject 削除）
- `Editor/Processing/PrefabExporter.cs` 新規追加
- asmdef の `versionDefines` に `nadena.dev.modular-avatar` 検出シンボル `CHM_MODULAR_AVATAR` を追加（MA 未導入でもコンパイル可能）

## v1.4.1

### バグ修正

- **Oklab 色合わせ: 単色寄りテクスチャでムラが出る問題を修正**
  - source の L 範囲が極端に狭いテクスチャ（フラットな色味のメッシュ）で、線形リマップの slope が大きくなり、元テクスチャの微小な陰影が増幅されてムラとして視認される現象を修正
  - sourceRange に応じて、線形リマップと加算オフセット方式を滑らかに切替えるロジックを導入（threshold = 0.20）
  - 通常の range のテクスチャでは従来挙動を維持

## v1.4.0

### 新機能

- **色合わせ Oklab アルゴリズム追加**
  - 色指定モードに「HSV（従来）」と「Oklab」の選択肢を追加
  - Oklab は輝度と彩度が独立した色空間で、複数テクスチャの色味を target に自動で揃えやすい
  - UV 使用領域の上位 5% 輝度を基準に線形リマップする設計
- **RGB差分モード（Mode 3）追加**
  - テクスチャ代表色から target への RGB差分を全ピクセルに加算
  - 色相を強制せず、ハイライト等のニュアンスを残したまま target に寄せたい用途向け
- **塗りの細かさを調整**
  - Renderer ごとにブラー（-1）〜 シャープ（+1）の前処理を適用可能
  - 髪テクスチャ間の塗り感（カリカリ vs 柔らかい）を揃えたい場合に使用
  - ブラー側はダウンサンプル方式で -1 では単色化レベルの強いブラー
- **UV 端のテクスチャ塗り足し（Edge Dilation）**
  - 色変換後にメッシュ UV 使用領域外を近隣色で拡張し、bilinear / mipmap で UV 端が暗くならないように
  - Compute Shader 実装で高速
- **UV 配置プレビュー拡大ビュー**
  - UV 配置エリアに「拡大ビュー」ボタンを追加
  - 別ウィンドウで大きなプレビューを開き、同じドラッグ操作が可能
- **アバター自動検出**
  - 対象選択エリアの「アバター」欄を廃止し、髪パーツから親 `VRCAvatarDescriptor` を自動検出
- **テクスチャ出力機能がメッシュ統合有効時にも利用可能に**
  - 適用時にメッシュ統合設定は無効化される（確認ダイアログあり）

### 改善

- **lilToon 輪郭線シェーダー切替に対応**
  - previewMaterial で輪郭線 ON/OFF を切り替えると、出力マテリアルの shader も自動で切り替わるように
- **Inspector UI 整理**
  - 色合わせ設定を枠付きで整理、サブセクション（個別の明るさ、塗りの細かさ、色合わせ無視マスク）も枠で区切り
  - 色変換モードを Mode 1 / Mode 2 / Mode 3 の順に統一
  - メッシュ変形機能のオン/オフトグルを削除（データがあれば動作）
- **ローカライズ拡充**
  - 新機能関連の文字列を 4 言語（ja-JP / en-US / zh-Hans / ko-KR）に対応
- **Oklab の Gamut Mapping を緩和**
  - わずかな float 誤差による過剰な彩度縮小を防ぐよう ε 閾値を導入
- **色変換キャッシュ構造を強化**
  - Pass 単位のピクセル読み込みキャッシュを導入し、同一テクスチャを複数サブメッシュで共有する場合の読み直しを削減

### 破壊的変更

- **輝度統一機能 (`BrightnessUnifyMode`) を廃止**
  - 代わりに「Oklab アルゴリズム」が自動で明度統一を行う
  - v1.3.2 以前で輝度統一を設定していたプロジェクトは、その設定が無視されます
- **`hueShiftAlgorithm` のデフォルトは Oklab**
  - 新規作成される CHM コンポーネントは Oklab で開始
  - 既存プロジェクト（v1.3.2 以前）はシリアライズの性質上 HSV 相当で開き、従来挙動を維持

### 内部整理

- `ColorTransformSettings.OklabSourceCDominant` の未使用フィールドを削除
- 旧 `BrightnessUnifyMode` 関連のローカライズキーを削除
- `OklabConverter` ユニットテストを追加（Tests/Editor）

## v1.3.2 以前

詳細は git history を参照。
