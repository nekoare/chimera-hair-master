using UnityEngine;

namespace ChimeraHairMaster
{
    /// <summary>
    /// メッシュ統合モード
    /// </summary>
    public enum MeshMergeMode
    {
        /// <summary>
        /// 独立 - 自前でメッシュ統合を実行
        /// </summary>
        [InspectorName("独立（自前で統合）")]
        Independent
    }
}
